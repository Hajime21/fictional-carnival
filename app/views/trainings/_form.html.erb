<%= form_with(model: @training, url: trainings_path, method: :post) do |form| %>
  <% if training.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(training.errors.count, 'error') %> prohibited this training from being saved:</h2>
      <ul>
        <% training.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= form.label :liczba_pompek %>
    <%= form.number_field :liczba_pompek %>
  </div>

  <div class="field">
  <%= form.label :data %>
  <%= form.date_field :data %>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>